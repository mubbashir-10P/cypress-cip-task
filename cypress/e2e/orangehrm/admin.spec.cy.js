describe("Demo test", ()=> {
    it("Should verify That 2 is equals to 2",() => {
        expect(2).to.equal(2)
    })
    it("Should verify That 3 is equals to 3",() => {
        expect(3).to.equal(3)
    })
})